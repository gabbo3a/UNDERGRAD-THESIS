% !TEX root = main.tex

% Languages and encode
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[italian]{babel}                         % Set the language to Italian
\usepackage{csquotes}                               % For language-sensitive quotations

% Graphics and bibliography
\usepackage{graphicx}                               % For images
    \graphicspath{{assets/}{../assets/}}
\usepackage[absolute,overlay]{textpos}              % For precise element positioning on the title page
\usepackage{hyperref}                               % For clickable links
\usepackage[style=numeric, backend=biber]{biblatex} % Bibliography management
    \addbibresource{references.bib}                 % Set bibliography file

% Layout and characters
\pagestyle{plain}
\usepackage{geometry}                               % For margins
    \geometry{a4paper, margin=3cm}                  % A4 paper and 3cm margins
\usepackage{lmodern}                                % Scalable modern fonts
\usepackage[absolute]{textpos}                      % For absolute positioning on title page
\setcounter{secnumdepth}{3}                         % Number up to the subsubsections
\setcounter{tocdepth}{3}                            % Show up to subsubsections in the index
\usepackage{enumitem}
\usepackage{float}

% Epigraph configuration (compact style)
\usepackage{epigraph}
\setlength{\epigraphwidth}{0.65\textwidth}        % Narrower width to balance the compact vertical spacing
\setlength{\epigraphrule}{0pt}                    % Removes the horizontal rule for a modern look
    \renewcommand{\epigraphflush}{center}         % Centers the entire epigraph block for visual symmetry
    \renewcommand{\epigraphsize}{\small\itshape}  % Sets the quote text font size to small and italic
    \renewcommand{\sourceflush}{flushright}       % Aligns the author's name to the right beneath the quote

% Theorem Environments with UniTo Style
\usepackage{amsmath, amssymb, amsthm}   % Mathematical packages
\theoremstyle{plain}
    \newtheorem{thm}{Theorem}[chapter]   % Main theorems, numbered by chapter (e.g., 1.1)
    \newtheorem{lem}[thm]{Lemma}         % Lemmas, sharing the same counter as theorems
    \newtheorem{prop}[thm]{Propositione} % Propositions, sharing the same counter as theorems
    \newtheorem{axi}[thm]{Assioma}       % Axioms, sharing the same counter as theorems

\theoremstyle{definition}
    \newtheorem{defn}[thm]{Definition}  % Definitions, sharing the counter with theorems
    \newtheorem{ex}[thm]{Example}       % Examples, numbered consistently with theorems
    \newtheorem{rmk}[thm]{Remark}       % Remarks or notes, numbered consistently
\usepackage{ebproof}                    % Rendering natural deduction (NJ) trees

% Code aspect
\usepackage{verbatim}               % Necessary for the environment comment
\usepackage{xcolor}                 % For colors in code and text
\usepackage{listings}               % Main package for code snippets
\usepackage{caption}                % Required for customizing captions of floating environments
    \captionsetup[lstlisting]{
        format=plain,               % Use a simple, non-indented format for captions
        justification=raggedright,  % Force the caption text to be left-aligned
        singlelinecheck=false,      % Prevent automatic centering of short (single-line) captions
        font=small,                 % Set caption text size slightly smaller than main text
        labelfont=bf                % Set the label (e.g., "Listing 1.1") in bold face
    }

% Def: Paragraph break
\newcommand{\breakpara}{\leavevmode\par\vspace{0.3em}}

% Def:    Ruleblock template
% Arg #1: Title (e.g., Implication)
% Arg #2: Tree Contents (minipage, test trees)
% Arg #3: Comment
\newcommand{\ruleblock}[3]{
    \noindent\rule{\textwidth}{0.4pt}
    \vspace{0.3em}
    \noindent \textbf{#1} 
    \begin{center} \small #2 \end{center}
    \vspace{-0.5em}
    \noindent {\small \textit{#3}}
    \vspace{1.5em}
}

% Unicode character resolution for pdfLaTeX (main text)
\DeclareUnicodeCharacter{03C6}{\ensuremath{\phi}}                % Risolve φ (U+03C6)
\DeclareUnicodeCharacter{03C3}{\ensuremath{\sigma}}              % Risolve σ (U+03C3)
\DeclareUnicodeCharacter{0393}{\ensuremath{\Gamma}}              % Risolve Γ (U+0393)
\DeclareUnicodeCharacter{25B8}{\ensuremath{\blacktriangleright}} % Risolve ▸ (U+25B8)
\DeclareUnicodeCharacter{2502}{\ensuremath{\vert}}               % Risolve │ (U+2502) 
\DeclareUnicodeCharacter{2500}{\ensuremath{-}}                   % Risolve ─ (U+2500)
\DeclareUnicodeCharacter{2514}{\ensuremath{\llcorner}}           % Risolve └ (U+2514)
\DeclareUnicodeCharacter{03C4}{\ensuremath{\tau}}                % Risolve τ (U+03C4)
\DeclareUnicodeCharacter{03C1}{\ensuremath{\rho}}                % Risolve ρ (U+03C1)
\DeclareUnicodeCharacter{03A3}{\ensuremath{\Sigma}}              % Risolve Σ (U+03A3)

% Lean Language Configuration (Safe for UTF-8)
\lstdefinelanguage{lean}{
    keywords={
        def,
        theorem,
        lemma,
        example,
        variable,
        instance,
        class,
        import,
        open,
        induction,
        cases, 
        simp, 
        rw, 
        apply, 
        exact, 
        intro, 
        show, 
        by},
    keywordstyle=\color{purple}\bfseries,
    comment=[l]{--},
    morecomment=[s]{/-}{-/},
    commentstyle=\color{gray}\itshape,
    basicstyle=\ttfamily\small,
    breaklines=true,
    extendedchars=false,
    literate={
        {α}{{$\alpha$}}1      {β}{{$\beta$}}1               {γ}{{$\gamma$}}1                {λ}{{$\lambda$}}1
        {→}{{$\to$}}1         {∀}{{$\forall$}}1             {∃}{{$\exists$}}1               {⊢}{{$\vdash$}}1
        {:=}{{$:=$}}1         {Π}{{$\Pi$}}1                 {Σ}{{$\Sigma$}}1                {∧}{{$\wedge$}}1
        {∨}{{$\vee$}}1        {¬}{{$\neg$}}1                {↔}{{$\leftrightarrow$}}1       {∘}{{$\circ$}}1
        {₀}{{$_0$}}1          {₁}{{$_1$}}1                  {₂}{{$_2$}}1                    {₃}{{$_3$}}1                    
        {⟨}{{$\langle$}}1     {⟩}{{$\rangle$}}1             {⦃}{{$\{\!\{$}}1                {⦄}{{$\}\!\}$}}1
        {φ}{{$\phi$}}1        {▸}{{$\blacktriangleright$}}1 {∷}{{$\mathrel{\because}$}}1    {σ}{{$\sigma$}}1      
        {Γ}{{$\Gamma$}}1      {…}{{$\dots$}}1               {τ}{{$\tau$}}1  
    } 
}